# Session Close-Out — 2026-07-25

**Session type:** Planning (Planner only — no Builder execution happened in this session)

## What got done

- Full architecture plan written and numbered per KEEL Steps 7/8: `docs/decisions/D-001`
  through `D-012`, `docs/README.md`, and `docs/ORDERS-FOR-BUILDER.md`.
- **Artefact:** owner confirmed these are committed and pushed — commit `794bcf5` on
  `main`. *(Not independently verified by the Planner — the Planner has no repo access.
  First thing tomorrow, confirm `git log -1 794bcf5` shows the expected files, per D-011:
  a claim without a checked artefact is a guess.)*
- `ARCHITECTURE.md` (root-level summary dashboard, points back to the `D-0XX` docs)
  was created in this session. **Not confirmed whether it's included in commit
  `794bcf5`** — it may have been created after that commit was made. Check `git log --
  ARCHITECTURE.md` tomorrow to confirm it's actually committed, don't assume.
- D-012 (local-first sequencing) was adopted, deferring Fly.io provisioning to Phase 7.
- Claude Code (the Builder) was handed the orders to execute Phase 2 onward, and
  correctly **paused** before touching anything account-level (Fly.io app creation,
  minting `FLY_API_TOKEN`, GitHub Actions secrets, branch protection) — this was the
  right call, not a failure, per the Planner's own guidance on watching account-touching
  steps happen live.
- Owner gave verbal go-ahead ("yes, go ahead phase by phase") for that work, but then
  pivoted to a cost question before any Phase 2 execution actually started.
- **No scaffolding has happened yet.** There is no evidence in this session that
  `create-next-app`, Prisma init, or any code was actually run. Don't assume Phase 2 is
  underway — it isn't yet.

## What's open (unresolved as of end of session)

1. **Hosting decision (D-013, not yet written).** The $40/mo figure the owner had heard
   didn't match Fly.io's actual current pricing (verified against `fly.io/docs/about/pricing`
   in-session — a minimal always-on app for this project's traffic is closer to
   $3-8/month, not $40). Three options were laid out, no decision made:
   - Fly.io pay-as-you-go (~$3-8/mo) — original D-006 plan, content unchanged
   - Laptop + Cloudflare Tunnel or Tailscale Funnel — $0, requires laptop on/connected
   - Oracle Cloud "Always Free" VM — $0, always-on, some signup/capacity flakiness
     reported
   - Render free tier was ruled out as a poor fit: its free-tier filesystem is
     ephemeral, which conflicts with D-003's SQLite-on-persistent-volume design.

2. **D-013 itself does not exist as a file yet.** It needs to be written once the owner
   picks a direction, then added to the decision index table in `ARCHITECTURE.md`.

## What's next (in order)

1. **Decide D-013 first** — quick decision, and it does *not* block anything else below,
   since D-012 already defers all hosting work to Phase 7.
2. **Have Claude Code start Phase 2** (scaffold Next.js + Prisma + local SQLite, per
   `docs/ORDERS-FOR-BUILDER.md`) — this can start immediately regardless of the D-013
   outcome, since it's purely local.
3. Continue through Phases 3-6 (self-contained tests, local secrets, test-only CI gate,
   then the actual features) before touching Phase 7 (whichever hosting option D-013
   lands on).
4. When Phase 7 is eventually reached, the 7a-7i checkpoint script already given to the
   owner in this session still applies, adjusted for whichever host D-013 picks.

## Artefacts referenced this session

- Commit `794bcf5` on `main` (owner-reported, not independently verified — see above)
- `https://fly.io/docs/about/pricing/` (fetched directly, used to correct the $40/mo
  figure)
- Web search results on Render's free-tier filesystem behavior and Oracle Cloud's
  Always Free tier status, used to evaluate hosting alternatives (not saved as a file —
  re-verify before finalizing D-013, since this is exactly the kind of external, fast-
  changing claim D-011 says to check fresh, not recall)
